appId: com.anonymous.TerraOffRoad
---
- launchApp

# Wait for app to load
- extendedWaitUntil:
    visible:
      text: ".*"
    timeout: 15000


# Wait for main screen to be ready
- extendedWaitUntil:
    visible:
      text: "Map"
    timeout: 10000
